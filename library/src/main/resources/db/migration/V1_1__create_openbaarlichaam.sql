create table openbaarlichaam
(
    id           bigint      not null primary key,
    code         varchar(48) not null,
    oin          varchar(24),
    lichaamtype  varchar(32) not null,
    naam         varchar(64),
    bestuurslaag varchar(32)
);

create index openbaarlichaam_code_idx on openbaarlichaam (code);

create index openbaarlichaam_oin_idx on openbaarlichaam (oin);

create index openbaarlichaam_lichaamtype_idx on openbaarlichaam (lichaamtype);



ALTER TABLE public.openbaarlichaam
    OWNER TO testuser;

ALTER TABLE public.openbaarlichaam
ALTER
COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
        SEQUENCE NAME public.openbaarlichaam_id_seq
        START WITH 1
        INCREMENT BY 1
        NO MINVALUE
        NO MAXVALUE
        CACHE 1
        );